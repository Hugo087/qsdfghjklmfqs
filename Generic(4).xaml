<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:GuidInput">

    <BooleanToVisibilityConverter x:Key="BoolToVis"/>

    <Style TargetType="{x:Type local:GuidTextBox}">
        <Setter Property="Background"               Value="#FFFFFF"/>
        <Setter Property="Foreground"               Value="#1E1E1E"/>
        <Setter Property="BorderBrush"              Value="#ABADB3"/>
        <Setter Property="BorderThickness"          Value="1"/>
        <Setter Property="Padding"                  Value="6,0"/>
        <Setter Property="Height"                   Value="26"/>
        <Setter Property="FontFamily"               Value="Consolas, Courier New"/>
        <Setter Property="FontSize"                 Value="13"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="SelectionBrush"           Value="#0078D4"/>
        <Setter Property="CaretBrush"               Value="#1E1E1E"/>
        <Setter Property="SnapsToDevicePixels"      Value="True"/>
        <Setter Property="FocusVisualStyle"         Value="{x:Null}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:GuidTextBox}">
                    <Border x:Name="PART_Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="2"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Placeholder -->
                            <TextBlock
                                x:Name="PART_Placeholder"
                                Grid.Column="0"
                                Text="{TemplateBinding PlaceholderText}"
                                Foreground="#C0C0C0"
                                FontFamily="{TemplateBinding FontFamily}"
                                FontSize="{TemplateBinding FontSize}"
                                VerticalAlignment="Center"
                                Margin="{TemplateBinding Padding}"
                                IsHitTestVisible="False"
                                Visibility="Collapsed"/>

                            <!-- Zone de saisie -->
                            <ScrollViewer
                                x:Name="PART_ContentHost"
                                Grid.Column="0"
                                Margin="0"
                                Padding="{TemplateBinding Padding}"
                                VerticalAlignment="Center"
                                Focusable="False"
                                HorizontalScrollBarVisibility="Hidden"
                                VerticalScrollBarVisibility="Hidden"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>

                            <!-- Icone de validation -->
                            <Border x:Name="PART_ValidIcon"
                                    Grid.Column="1"
                                    Width="20"
                                    Margin="0,0,4,0"
                                    VerticalAlignment="Center"
                                    Visibility="Collapsed">
                                <TextBlock
                                    Text="&#x2713;"
                                    Foreground="#107C10"
                                    FontSize="12"
                                    FontWeight="Bold"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>

                        <!-- Placeholder visible quand vide -->
                        <DataTrigger
                            Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}"
                            Value="">
                            <Setter TargetName="PART_Placeholder" Property="Visibility" Value="Visible"/>
                        </DataTrigger>

                        <!-- Focus : bordure bleue -->
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="PART_Border" Property="BorderBrush"     Value="#0078D4"/>
                            <Setter TargetName="PART_Border" Property="BorderThickness" Value="1.5"/>
                        </Trigger>

                        <!-- GUID valide + non focuse : bordure verte + icone -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsValidGuid"       Value="True"/>
                                <Condition Property="IsKeyboardFocused" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PART_Border"    Property="BorderBrush" Value="#107C10"/>
                            <Setter TargetName="PART_ValidIcon" Property="Visibility"  Value="Visible"/>
                        </MultiTrigger>

                        <!-- GUID valide + focuse : icone visible, bordure reste bleue -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsValidGuid"       Value="True"/>
                                <Condition Property="IsKeyboardFocused" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PART_ValidIcon" Property="Visibility" Value="Visible"/>
                        </MultiTrigger>

                        <!-- Desactive -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="PART_Border" Property="Background"  Value="#F5F5F5"/>
                            <Setter TargetName="PART_Border" Property="BorderBrush" Value="#CCCCCC"/>
                            <Setter Property="Foreground"                            Value="#A0A0A0"/>
                        </Trigger>

                        <!-- Lecture seule -->
                        <Trigger Property="IsReadOnly" Value="True">
                            <Setter TargetName="PART_Border" Property="Background" Value="#FAFAFA"/>
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
